/**
 * Created by dhkim on 2017-05-12.
 */
/*
 const app = require('../../../#10_TDD_index');
 const syncDb = require('./sync-db');

 syncDb().then(() => {
 console.log('sync database!');
 app.listen(3000, () => {
 console.log('Server is running on 3000 port')
 });
 });

 */
